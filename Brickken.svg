<!DOCTYPE html><!-- Last Published: Tue Mar 24 2026 14:57:11 GMT+0000 (Coordinated Universal Time) --><html data-wf-domain="www.brickken.com" data-wf-page="680a2df699085611f5f9230d" data-wf-site="680a2df699085611f5f9221a" lang="en"><head><meta charset="utf-8"/><title>Not Found</title><link rel="alternate" hrefLang="x-default" href="https://www.brickken.com/404"/><link rel="alternate" hrefLang="en" href="https://www.brickken.com/404"/><link rel="alternate" hrefLang="es" href="https://www.brickken.com/es/404"/><link rel="alternate" hrefLang="fr-FR" href="https://www.brickken.com/fr/404"/><link rel="alternate" hrefLang="pt-BR" href="https://www.brickken.com/pt/404"/><meta content="Not Found" property="og:title"/><meta content="Not Found" property="twitter:title"/><meta content="width=device-width, initial-scale=1" name="viewport"/><link href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/css/brickken-55bc39-4f42911e839e82ea9ed3649.webflow.shared.f6cbcb386.css" rel="stylesheet" type="text/css" integrity="sha384-9svLOGntAuPY1+024nDbx14ELAm6JWoXegI/2JNfOalQd7JTncjO1BHqy+5M1xE0" crossorigin="anonymous"/><script type="text/javascript">!function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch&&c instanceof DocumentTouch)&&(n.className+=t+"touch")}(window,document);</script><link href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/6814ad8d66859eaa979485fb_Favicon%2032x32.png" rel="shortcut icon" type="image/x-icon"/><link href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/680a2df699085611f5f9223b_BKN_256x256.png" rel="apple-touch-icon"/><link href="https://www.brickken.com/404" rel="canonical"/><meta name="robots" content="index, follow">

<!-- Facebook (solo si usas Pixel u otro script) -->
<link rel="preconnect" href="https://connect.facebook.net" crossorigin>

<!-- Google Tag Manager -->
<link rel="preconnect" href="https://www.googletagmanager.com" crossorigin>

<!-- Preload Satoshi Font -->
 <link
  rel="preload"
  as="font"
  href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/686cbfc5fc181d20f1b3bcaa_Satoshi-Light.woff2"
  type="font/woff2"
  crossorigin
>
<!-- Preload Satoshi Font -->
 <link
  rel="preload"
  as="font"
  href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/686cbf7d108ffa0d52aef5a2_Satoshi-Regular.woff2"
  type="font/woff2"
  crossorigin
>
<!-- Preload Satoshi Font -->
 <link
  rel="preload"
  as="font"
  href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/686cbfa1064bb19e4100001c_Satoshi-Medium.woff2"
  type="font/woff2"
  crossorigin
>
<!-- Preload Satoshi Font -->
 <link
  rel="preload"
  as="font"
  href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/686cbfdbfbde1c00a2781457_Satoshi-Italic.woff2"
  type="font/woff2"
  crossorigin
>
<!-- Preload Satoshi Font -->
 <link
  rel="preload"
  as="font"
  href="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/686cbffc160f1726d5b14c8f_Satoshi-Bold.woff2"
  type="font/woff2"
  crossorigin
>
<!-- - - - - - - - - - - - -->

<!-- Google Tag Manager -->
<script>
    // create dataLayer
    window.dataLayer = window.dataLayer || [];
    function gtag() {
        dataLayer.push(arguments);
    }
    // set default values for Consent Mode keys
    gtag("consent", "default", {
        ad_user_data: "denied",
        ad_personalization: "denied",
        ad_storage: "denied",
        analytics_storage: "denied",
        wait_for_update: 2000 // milliseconds to wait for update
    });
    // Enable ads data redaction by default [optional]
    gtag("set", "ads_data_redaction", true);
</script>

<script type="text/javascript">
    // Google Tag Manager
    (function(w, d, s, l, i) {
        w[l] = w[l] || [];
        w[l].push({
            'gtm.start': new Date().getTime(),
            event: 'gtm.js'
        });
        var f = d.getElementsByTagName(s)[0],
            j = d.createElement(s),
            dl = l != 'dataLayer' ? '&l=' + l : '';
        j.async = true;
        j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
        f.parentNode.insertBefore(j, f);
    })(window, document, 'script', 'dataLayer', 'GTM-T6MZ2JQ');
</script>
<!-- End Google Tag Manager -->

<!-- HS cookie blocker by CookieFirst Consent Event -->
<script>
window.addEventListener('load', function() {
  // Encuentra todos los enlaces de Google Fonts y los desactiva
  var links = document.getElementsByTagName('link');
  for (var i = 0; i < links.length; i++) {
    var link = links[i];
    if (link.href.includes('fonts.googleapis.com/css')) {
      link.disabled = true;
    }
  }
});
</script>


<style>
  
/* Ajustes carousel para ios */
  
  .companies-logos-group {
    will-change: transform;
    transform: translate3d(0, 0, 0);
    backface-visibility: hidden;
  }
  .carousel-image {
    flex-shrink: 0;
  }
  
/* 	--------------------	*/
  
/* 	--------------------	*/
  
  body {
    overflow-x: hidden;
  }

  body::-webkit-scrollbar {
    display: none; /* Chrome, Safari y Opera */
  }

/* 	Scrolling logos section fix for safari	*/
  
  .companies-logos-group {
      will-change: transform;
      transform: transform-style: preserve-3d;
    }

  .w-slider-dot {
    position: relative;
    display: inline-block;
    width: 5rem;
    height: 0.5rem;
    border-radius: 7rem;
    background-color: rgba(191, 201, 252, 0.28);
    cursor: pointer;
    transition: background-color 100ms, color 100ms;
  }
  .w-slider-dot.w-active {
    background-color: #4C82DF;
  }

  .value-cards-slider {
    overflow-x: visible !important; 
    overflow-y: visible !important;
  }

  @media (min-width: 991px) {
    .value-cards-slider {
      overflow-x: hidden !important;
      overflow-y: visible !important;
    }
  }

<!-- Jetboost -->
  .jb-search-highlight {
    background: #ffff00;
    color: #000000;
  }

<!-- Scrollbar -->
  ::-webkit-scrollbar {
  width: 6px;
  height: 6px !important;
}

::-webkit-scrollbar-track {
  background: rgba(191, 201, 252, 0.28); 
  border-radius: 10px;
}

::-webkit-scrollbar-thumb {
  background: #4C82DF; 
  border-radius: 10px;
}
  

/* 	Need review to be deleted	*/
<!-- Hide CTA Navbar-->
   .navs {
    transition: top 0.3s ease;
  }

  .nav-hidden {
  
  .asidenav-nav-hidden {
    top: 80px !important;
  }
  
  .asidenav-nav-visible {
    top: calc(var(--cta-fixed-aside-nav) + 80px) !important;
  }

  .custom-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    z-index: 9999;
    display: none;
    align-items: center;
    justify-content: center;
  }

  .custom-dialog {
    border: none;
    border-radius: 12px;
    background: white;
    min-width: 60vw;
    max-height: 70vh;
    padding: 0;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    margin-top: 10rem;

  }

  .custom-modal-inner {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem;
    max-height: 60vh;
  }

  .custom-modal-close {
    align-self: flex-end;
    background: black;
    color: white;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    cursor: pointer;
    font-size: 0.9rem;
  }

  /* Ocultar en el Webflow Designer */
  body.w-editor .custom-overlay {
    display: none !important;
  }
  
  .blog-block.w-richtext {
  max-width: 100%;
  width: 100%;
}

.blog-block.w-richtext .w-embed {
  max-width: 100%;
  width: 100%;
  display: block;
  overflow-x: auto; /* Habilita scroll horizontal si es necesario */
  -webkit-overflow-scrolling: touch;
}

.blog-block.w-richtext .w-embed table {
  width: 100%; /* La obliga a escalar */
  table-layout: auto; /* Para que se ajuste a su contenido */
}

.blog-block.w-richtext .w-embed td,
.blog-block.w-richtext .w-embed th {
  white-space: normal;
}
 
</style></head><body><div class="utility-page-wrap"><div class="utility-page-content"><img src="https://cdn.prod.website-files.com/static/page-not-found.211a85e40c.svg" alt=""/><h2>Page Not Found</h2><div>The page you are looking for doesn&#x27;t exist or has been moved</div></div></div><script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=680a2df699085611f5f9221a" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script><script src="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/js/webflow.schunk.36b8fb49256177c8.js" type="text/javascript" integrity="sha384-4abIlA5/v7XaW1HMXKBgnUuhnjBYJ/Z9C1OSg4OhmVw9O3QeHJ/qJqFBERCDPv7G" crossorigin="anonymous"></script><script src="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/js/webflow.schunk.a802a08531c988d6.js" type="text/javascript" integrity="sha384-TfNKMiexAU1/vqpxlzv/aSRWIG3tkczVsto9utLOXGjdb6hvT28W2VgecfdxxoUf" crossorigin="anonymous"></script><script src="https://cdn.prod.website-files.com/680a2df699085611f5f9221a/js/webflow.e02ec84c.ac6710541f54295b.js" type="text/javascript" integrity="sha384-6N5ooLfLVtAvGV+k4YIYOUnm/iXup/+bKBImiWLGbR8oz3kLpGq7yfqk4XMACPgI" crossorigin="anonymous"></script><script src="https://cdn.prod.website-files.com/gsap/3.14.2/gsap.min.js" type="text/javascript"></script><script defer src="https://cdnjs.cloudflare.com/ajax/libs/js-cookie/2.2.1/js.cookie.min.js"></script>
<script defer src="https://cdn.jsdelivr.net/npm/lumious-components@1.0.6/component/ticker.js"></script>
<script defer src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script defer src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>

<script defer>
  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('a[href$="?r=0"]').forEach((a) => {
      a.href = a.href.replace('?r=0', '');
    });
  });
</script>

<!--This code waits until the window has finished loading all resources before executing the popup code-->
<script defer>
  const delay = 60; // pop-up delay in seconds
  const popupId = 'pop-up-wrapper'; // pop-up Id name

  window.addEventListener('load', function () {
    if (typeof esk === 'function') esk('track', 'Conversion');

    if (!Cookies.get('alert')) {
      setTimeout(function () {
        var popup = document.getElementById(popupId);
        if (popup) {
          popup.style.display = 'flex';
          Cookies.set('alert', true, { expires: 1 });
        }
      }, delay * 1000);
    }
  });
</script>
<!--END-->

<!-- Prevent HubSpot from tracking non-HubSpot forms -->
<script defer>
  window._hsq = window._hsq || [];
  window._hsq.push(['setFormsIgnored', true]);
</script>

<!-- Google Tag Manager (noscript) -->
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-T6MZ2JQ" height="0" width="0" style="display: none; visibility: hidden"></iframe></noscript>
<!-- End Google Tag Manager (noscript) -->

<!--AXEPTIO SCRIPT
<script>
window.axeptioSettings = {
  clientId: "65799af44ec877aab12bde8d",
  cookiesVersion: "brickken-en",
};

(function(d, s) {
  var t = d.getElementsByTagName(s)[0], e = d.createElement(s);
  e.defer = true; e.src = "//static.axept.io/sdk.js";
  t.parentNode.insertBefore(e, t);
})(document, "script");
</script>
END OF AXEPTIO SCRIPT -->

<script defer>
  // Close navbar menu when mouse leaves it
  $('.nav-link-menu.wrapper').on('mouseleave', function (event) {
    // Get the mouse position and the bottom of the container.
    const menuOffset = $(this).offset();
    const menuHeight = $(this).outerHeight();
    const menuBottom = menuOffset.top + menuHeight;

    // If the mouse is exiting downward, close the menu.
    if (event.pageY > menuBottom) {
      requestAnimationFrame(() => {
        $('.nav-link-menu.w-dropdown-list').removeClass('w--open');
      });
    }
  });

  const $dialog = $('.custom-dialog.custom-hiw');
  const $overlay = $('.custom-overlay');
  const $iframe = $dialog.find('iframe');

  // Abrir el modal con video dinámico
  $(document).on('click', '.cta-hiw', function (e) {
    e.preventDefault();
    const videoSrc = $(this).data('video');

    if (videoSrc) {
      // Agrega autoplay
      const autoplaySrc = videoSrc + (videoSrc.includes('?') ? '&' : '?') + 'autoplay=1';
      $iframe.attr('src', autoplaySrc);
      $overlay.fadeIn(200);
      $dialog.prop('open', true);
    }
  });

  // Cerrar el modal al hacer clic en overlay
  $(document).on('click', function (e) {
    if ($(e.target).is('.custom-overlay')) {
      closeModal();
    }
  });

  // Cerrar con botón
  $(document).on('click', '.custom-modal-close', function () {
    closeModal();
  });

  // Función de cerrar y limpiar iframe
  function closeModal() {
    if ($dialog.prop('open')) {
      $iframe.attr('src', '');
      $dialog.removeAttr('open');
      $overlay.fadeOut(200);
    }
  }

  function updateNavbarMargin() {
    requestAnimationFrame(() => {
      const ctaBar = document.querySelector('.fixed-cta-navbar');
      const navbar = document.querySelector('.navbar');
      const navs = document.querySelector('.navs');
      const asideNav = document.querySelector('.aside-nav');
      const goBackBtnWrapper = document.querySelector('.go-back-btn-wrapper');

      if (!ctaBar || !navbar || !navs) return;

      const height = ctaBar.offsetHeight - 2;

      navbar.style.marginTop = height + 'px';
      navs.style.setProperty('--cta-fixed-navbar-height', height + 'px');

      if (asideNav) {
        asideNav.style.setProperty('--cta-fixed-aside-nav', height + 'px');
        asideNav.classList.add('asidenav-nav-visible');
      }
      if (goBackBtnWrapper) {
        goBackBtnWrapper.style.setProperty('--cta-fixed-aside-nav', height + 'px');
        goBackBtnWrapper.classList.add('asidenav-nav-visible');
      }
    });
  }

  window.addEventListener('load', updateNavbarMargin);
  // Volver a calcular en resize (por si cambia en móviles)
  window.addEventListener('resize', () => {
    cachedNavsHeight = null;
    updateNavbarMargin();
  });

  document.addEventListener('DOMContentLoaded', function () {
    //Autoscroll
    const ctaBar = document.querySelector('.fixed-cta-navbar');
    const navbar = document.querySelector('.navbar');
    const asideNav = document.querySelector('.aside-nav');

    function getOffset(rem) {
      const remInPx = parseFloat(getComputedStyle(document.documentElement).fontSize);
      return rem * remInPx;
    }

    function isMobile() {
      return window.innerWidth <= 992; // ajusta según tu breakpoint
    }

    let cachedNavsHeight = null;

    function getNavsHeight() {
      if (cachedNavsHeight !== null) return cachedNavsHeight;

      let totalHeight = 0;

      if (navbar) {
        totalHeight += ctaBar.offsetHeight;
        totalHeight += navbar.offsetHeight;
      }

      if (asideNav && isMobile()) {
        totalHeight += asideNav.offsetHeight;
      }

      totalHeight += getOffset(0.5);

      cachedNavsHeight = totalHeight;
      return totalHeight;
    }

    const scrollLinks = document.querySelectorAll('[data-scroll]');

    // Scroll personalizado al hacer clic
    scrollLinks.forEach((anchor) => {
      anchor.addEventListener('click', function (e) {
        const targetId = this.getAttribute('data-scroll');
        const targetElement = document.getElementById(targetId);

        if (targetElement) {
          setTimeout(() => {
            const yOffset = getNavsHeight();
            const yPosition = targetElement.getBoundingClientRect().top + window.pageYOffset - yOffset;

            window.scrollTo({
              top: yPosition,
              behavior: 'smooth',
            });

            history.pushState(null, null, ' ');
          }, 50);
        }
      });
    });

    // Detectar qué sección está visible y activar su link
    const observerOptions = {
      root: null,
      rootMargin: isMobile ? '-40% 0px -60% 0px' : '-25% 0px -75% 0px',
      threshold: 0,
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const sectionId = entry.target.id;

          scrollLinks.forEach((link) => {
            link.classList.toggle('is-active', link.getAttribute('data-scroll') === sectionId);
          });
        }
      });
    }, observerOptions);

    // Observar todas las secciones con ID
    document.querySelectorAll('[id]').forEach((section) => {
      observer.observe(section);
    });
  });
</script>

<script defer id="hs-script-loader" src="//js.hs-scripts.com/8184132.js"></script>

<script>
  (function () {
    // Variable if page should reload to reinit hubspot
    var shouldReload = false;

    function callbackFnc(e) {
      var consent = e.detail || {};

      var _hsq = (window._hsq = window._hsq || []);
      var _hsp = (window._hsp = window._hsp || []);

      // Check if functional is accepted replace functional with: performance or advertising to use a different category
      if (!consent.functional) {
        // If not
        // Either remove all Hubspot cookies or leave them and set doNotTrack cookie
        // _hsq.push(["doNotTrack"]);
        // _hsp.push(["doNotTrack"]);

        _hsp.push(['revokeCookieConsent']);
        _hsq.push(['revokeCookieConsent']);

        shouldReload = true;
      } else {
        _hsp.push(['doNotTrack', { track: true }]);
        _hsq.push(['doNotTrack', { track: true }]);

        if (shouldReload) {
          window.location.reload();
        }
      }
    }

    window.addEventListener('cf_consent', callbackFnc);
    window.addEventListener('cf_consent_loaded', callbackFnc);
  })();
</script>

<!-- Logos Carousel-->
<!--<script defer src="https://cdn.jsdelivr.net/npm/@finsweet/cookie-consent@1/fs-cc.js" fs-cc-mode="informational"></script> -->
<script defer>
  document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.companies-logos-group img').forEach((img) => {
      img.setAttribute('loading', 'eager');
    });
  });
</script>

<!--  GSAP Animations -->
<script defer>
  document.addEventListener('DOMContentLoaded', (event) => {
    gsap.registerPlugin(ScrollTrigger);

    const counters = document.querySelectorAll('.counter-item');

    counters.forEach((counter) => {
      let finalValue = parseFloat(counter.textContent.replace(/,/g, ''));

      if (isNaN(finalValue)) return;

      counter.textContent = '0';

      let counterProxy = { val: 0 };

      gsap.to(counterProxy, {
        val: finalValue,
        duration: 2.5,
        ease: 'power1.out',
        scrollTrigger: {
          trigger: counter,
          start: 'top 85%',
          once: true,
        },
        onUpdate: function () {
          counter.textContent = Math.ceil(this.targets()[0].val);
        },
      });
    });
  });
</script>
<!--  End of GSAP Animations -->

<!--  Autoscroll + navbar height -->
<style>
  /* Evita que el scroll nativo interfiera con nuestro cálculo manual */
  html {
    scroll-behavior: auto !important;
  }
</style>

<script defer>
  window.addEventListener(
    'click',
    (e) => {
      // 1. Detectamos si el click fue en un enlace interno (que empieza por #)
      const anchor = e.target.closest('a[href^="#"]');
      if (!anchor) return;

      const targetId = anchor.getAttribute('href');
      if (targetId === '#') return;

      const targetElement = document.querySelector(targetId);

      if (targetElement) {
        // 2. Detenemos el salto instantáneo de Webflow/Navegador
        e.preventDefault();

        // 3. Calculamos el offset de 4.5rem
        const offsetRem = 4.5;
        const rootFontSize = parseFloat(getComputedStyle(document.documentElement).fontSize);
        const offsetPx = offsetRem * rootFontSize;

        // 4. Calculamos la posición exacta
        const elementPosition = targetElement.getBoundingClientRect().top;
        const offsetPosition = elementPosition + window.pageYOffset - offsetPx;

        // 5. Ejecutamos el scroll suave manual
        window.scrollTo({
          top: offsetPosition,
          behavior: 'smooth',
        });

        // 6. Actualizamos la URL (opcional, sin saltar)
        history.pushState(null, null, targetId);
      }
    },
    { capture: true },
  ); // Usamos capture para adelantarnos a otros scripts
</script>
<!--  End of Autoscroll adjustment --></body></html>